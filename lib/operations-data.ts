import "server-only";
import { getDashboardContext } from "@/lib/auth/context"; import { vehicleLabel } from "@/lib/crm"; import { createClient } from "@/lib/supabase/server";
export async function getVisitChoices(customerQ?:string){const[{activeMembership},supabase]=await Promise.all([getDashboardContext(),createClient()]);const q=customerQ?.trim().replace(/[,%()]/g," ").slice(0,100);let customerQuery=supabase.from("customers").select("id,full_name").eq("organization_id",activeMembership.organizationId).eq("is_archived",false).order("full_name").limit(q?50:25);if(q)customerQuery=customerQuery.or(`full_name.ilike.%${q}%,phone.ilike.%${q}%,email.ilike.%${q}%`);const[{data:customers},{data:vehicles},{data:services},{data:staff},{data:resources}]=await Promise.all([customerQuery,supabase.from("vehicles").select("id,customer_id,make,model,model_year,plate_number").eq("organization_id",activeMembership.organizationId).eq("is_archived",false).order("make").limit(250),supabase.from("services").select("id,name,base_price_centavos,duration_minutes").eq("organization_id",activeMembership.organizationId).eq("is_active",true).order("name").limit(250),supabase.from("staff_directory").select("membership_id,full_name,branch_ids").eq("organization_id",activeMembership.organizationId).eq("is_active",true),supabase.from("scheduling_resources").select("id,name,branch_id,capacity").eq("organization_id",activeMembership.organizationId).eq("is_active",true).order("name")]);return{activeMembership,customers:(customers??[]).map(c=>({id:c.id,name:c.full_name})),vehicles:(vehicles??[]).map(v=>({id:v.id,customer_id:v.customer_id,label:vehicleLabel(v)})),services:services??[],staff:(staff??[]).map(member=>({id:member.membership_id,name:member.full_name??"Staff member",branchIds:member.branch_ids??[]})),resources:resources??[]}}
