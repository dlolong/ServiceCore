import assert from"node:assert/strict";import test from"node:test";import{inviteStaffSchema,roleHasPermission,updateStaffSchema}from"../lib/rbac";
const owner="29000000-0000-4000-8000-000000000001",branch="49000000-0000-4000-8000-000000000001";
test("central permission matrix preserves role boundaries",()=>{assert.equal(roleHasPermission("owner","staff.manage"),true);assert.equal(roleHasPermission("manager","staff.manage"),false);assert.equal(roleHasPermission("technician","jobs.execute"),true);assert.equal(roleHasPermission("cashier","payments.record"),true);assert.equal(roleHasPermission("viewer","customers.write"),false)});
test("staff invitations validate role, email, branch IDs, and lifetime",()=>{assert.equal(inviteStaffSchema.safeParse({email:"Staff@Example.com",role:"advisor",branchIds:[branch],expiresHours:"72"}).success,true);assert.equal(inviteStaffSchema.safeParse({email:"bad",role:"owner",branchIds:[owner],expiresHours:"999"}).success,false)});
test("staff updates cannot request owner role",()=>{assert.equal(updateStaffSchema.safeParse({membershipId:"39000000-0000-4000-8000-000000000002",role:"owner",isActive:true,branchIds:[]}).success,false);assert.equal(updateStaffSchema.safeParse({membershipId:"39000000-0000-4000-8000-000000000002",role:"viewer",isActive:false,branchIds:[branch]}).success,true)});
