import Link from "next/link";

import { saveBranch, saveCustomer, saveVehicle } from "@/app/dashboard/crm-actions";
import { FormMessage } from "@/components/form-message";
import { SubmitButton } from "@/components/submit-button";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";

const textarea = "mt-2 min-h-24 w-full rounded-xl border border-zinc-200 bg-white px-3 py-2 text-zinc-950 placeholder:text-zinc-400";
const select = "mt-2 min-h-11 w-full rounded-xl border border-zinc-200 bg-white px-3 text-zinc-950";
const Field = ({ label, children }: { label: string; children: React.ReactNode }) => <label className="block text-sm font-semibold text-zinc-800">{label}{children}</label>;

export type BranchRecord = { id: string; name: string; address_line: string | null; barangay: string | null; city: string | null; province: string | null; postal_code: string | null; country: string; phone: string | null; email: string | null; opening_notes: string | null };
export function BranchForm({ branch, error }: { branch?: BranchRecord; error?: string }) {
  return <Card className="p-5 sm:p-7"><FormMessage error={error}/><form action={saveBranch} className="mt-5 grid gap-5 sm:grid-cols-2">{branch && <input type="hidden" name="id" value={branch.id}/>}<Field label="Branch name *"><Input name="name" required maxLength={120} defaultValue={branch?.name}/></Field><Field label="Country *"><Input name="country" required defaultValue={branch?.country ?? "Philippines"}/></Field><div className="sm:col-span-2"><Field label="Address line *"><Input name="addressLine" required maxLength={200} defaultValue={branch?.address_line ?? ""}/></Field></div><Field label="Barangay"><Input name="barangay" maxLength={120} defaultValue={branch?.barangay ?? ""}/></Field><Field label="City / municipality *"><Input name="city" required maxLength={120} defaultValue={branch?.city ?? ""}/></Field><Field label="Province *"><Input name="province" required maxLength={120} defaultValue={branch?.province ?? ""}/></Field><Field label="Postal code"><Input name="postalCode" maxLength={20} defaultValue={branch?.postal_code ?? ""}/></Field><Field label="Phone"><Input name="phone" autoComplete="tel" maxLength={40} defaultValue={branch?.phone ?? ""}/></Field><Field label="Email"><Input name="email" type="email" autoComplete="email" defaultValue={branch?.email ?? ""}/></Field><div className="sm:col-span-2"><Field label="Opening hours notes"><textarea className={textarea} name="openingNotes" maxLength={500} defaultValue={branch?.opening_notes ?? ""}/></Field></div><div className="flex gap-3 sm:col-span-2"><SubmitButton pendingText="Saving…">Save branch</SubmitButton><Link href="/dashboard/settings/branches" className="inline-flex min-h-11 items-center px-3 text-sm font-bold text-zinc-700">Cancel</Link></div></form></Card>;
}

export type CustomerRecord = { id: string; full_name: string; phone: string | null; email: string | null; address_line: string | null; city: string | null; province: string | null; notes: string | null };
export function CustomerForm({ customer, error, warning, duplicateId }: { customer?: CustomerRecord; error?: string; warning?: string; duplicateId?: string }) {
  return <Card className="p-5 sm:p-7"><FormMessage error={error}/>{warning && <div role="alert" className="mt-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-900"><p className="font-bold">{warning}</p>{duplicateId && <Link className="mt-2 inline-block underline" href={`/dashboard/customers/${duplicateId}`}>View existing customer</Link>}<label className="mt-3 flex gap-2"><input type="checkbox" name="acceptDuplicate" form="customer-form"/> Save anyway</label></div>}<form id="customer-form" action={saveCustomer} className="mt-5 grid gap-5 sm:grid-cols-2">{customer && <input type="hidden" name="id" value={customer.id}/>}<div className="sm:col-span-2"><Field label="Full name *"><Input name="fullName" required autoComplete="name" maxLength={160} defaultValue={customer?.full_name}/></Field></div><Field label="Mobile number"><Input name="phone" autoComplete="tel" maxLength={40} placeholder="0917 123 4567" defaultValue={customer?.phone ?? ""}/></Field><Field label="Email"><Input name="email" type="email" autoComplete="email" defaultValue={customer?.email ?? ""}/></Field><div className="sm:col-span-2"><Field label="Address"><Input name="addressLine" autoComplete="street-address" maxLength={200} defaultValue={customer?.address_line ?? ""}/></Field></div><Field label="City / municipality"><Input name="city" maxLength={120} defaultValue={customer?.city ?? ""}/></Field><Field label="Province"><Input name="province" maxLength={120} defaultValue={customer?.province ?? ""}/></Field><div className="sm:col-span-2"><Field label="Notes"><textarea className={textarea} name="notes" maxLength={2000} defaultValue={customer?.notes ?? ""}/></Field></div><div className="flex gap-3 sm:col-span-2"><SubmitButton pendingText="Saving…">Save customer</SubmitButton><Link href={customer ? `/dashboard/customers/${customer.id}` : "/dashboard/customers"} className="inline-flex min-h-11 items-center px-3 text-sm font-bold text-zinc-700">Cancel</Link></div></form></Card>;
}

export type VehicleRecord = { id: string; customer_id: string; make: string; model: string; plate_number: string | null; model_year: number | null; variant: string | null; color: string | null; vehicle_type: string | null; fuel_type: string | null; transmission: string | null; odometer_km: number | null; vin: string | null; engine_number: string | null; notes: string | null };
export function VehicleForm({ vehicle, customers, presetCustomerId, error, warning, duplicateId }: { vehicle?: VehicleRecord; customers: {id:string;full_name:string}[]; presetCustomerId?: string; error?: string; warning?: string; duplicateId?: string }) {
  return <Card className="p-5 sm:p-7"><FormMessage error={error}/>{warning && <div role="alert" className="mt-5 rounded-xl bg-amber-50 p-4 text-sm text-amber-900"><p className="font-bold">{warning}</p>{duplicateId && <Link className="mt-2 inline-block underline" href={`/dashboard/vehicles/${duplicateId}`}>View existing vehicle</Link>}<label className="mt-3 flex gap-2"><input type="checkbox" name="acceptDuplicate" form="vehicle-form"/> Save anyway</label></div>}<form id="vehicle-form" action={saveVehicle} className="mt-5 grid gap-5 sm:grid-cols-2">{vehicle && <input type="hidden" name="id" value={vehicle.id}/>}<div className="sm:col-span-2"><Field label="Customer *"><select className={select} name="customerId" required defaultValue={vehicle?.customer_id ?? presetCustomerId ?? ""}><option value="" disabled>Select customer</option>{customers.map((customer) => <option key={customer.id} value={customer.id}>{customer.full_name}</option>)}</select></Field><Link href="/dashboard/customers/new" className="mt-2 inline-block text-sm font-bold text-amber-800">+ Add new customer</Link></div><Field label="Make *"><Input name="make" required maxLength={80} placeholder="Toyota" defaultValue={vehicle?.make}/></Field><Field label="Model *"><Input name="model" required maxLength={80} placeholder="Fortuner" defaultValue={vehicle?.model}/></Field><Field label="Year"><Input name="modelYear" type="number" min={1900} max={new Date().getFullYear()+1} defaultValue={vehicle?.model_year ?? ""}/></Field><Field label="Variant"><Input name="variant" maxLength={80} defaultValue={vehicle?.variant ?? ""}/></Field><Field label="Plate number"><Input name="plateNumber" maxLength={30} placeholder="ABC 1234" defaultValue={vehicle?.plate_number ?? ""}/></Field><Field label="Vehicle type"><select className={select} name="vehicleType" defaultValue={vehicle?.vehicle_type ?? ""}><option value="">Not specified</option>{["Sedan","Hatchback","SUV","Crossover","Pickup","Van","MPV","Coupe","Sports Car","Motorcycle","Scooter","Truck","Other"].map((type)=><option key={type}>{type}</option>)}</select></Field><Field label="Color"><Input name="color" maxLength={60} defaultValue={vehicle?.color ?? ""}/></Field><Field label="Odometer (km)"><Input name="odometerKm" type="number" min={0} defaultValue={vehicle?.odometer_km ?? ""}/></Field><Field label="Fuel type"><Input name="fuelType" maxLength={40} defaultValue={vehicle?.fuel_type ?? ""}/></Field><Field label="Transmission"><Input name="transmission" maxLength={40} defaultValue={vehicle?.transmission ?? ""}/></Field><Field label="VIN / chassis number"><Input name="vin" maxLength={80} defaultValue={vehicle?.vin ?? ""}/></Field><Field label="Engine number"><Input name="engineNumber" maxLength={80} defaultValue={vehicle?.engine_number ?? ""}/></Field><div className="sm:col-span-2"><Field label="Notes"><textarea className={textarea} name="notes" maxLength={2000} defaultValue={vehicle?.notes ?? ""}/></Field></div><div className="flex gap-3 sm:col-span-2"><SubmitButton pendingText="Saving…">Save vehicle</SubmitButton><Link href={vehicle ? `/dashboard/vehicles/${vehicle.id}` : "/dashboard/vehicles"} className="inline-flex min-h-11 items-center px-3 text-sm font-bold text-zinc-700">Cancel</Link></div></form></Card>;
}
