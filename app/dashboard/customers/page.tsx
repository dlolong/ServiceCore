import Link from "next/link";
import { Plus, Search } from "lucide-react";
import { FormMessage } from "@/components/form-message"; import { Button } from "@/components/ui/button"; import { Card } from "@/components/ui/card"; import { getDashboardContext } from "@/lib/auth/context"; import { displayPhone } from "@/lib/crm"; import { createClient } from "@/lib/supabase/server";
const PAGE_SIZE=20;
export default async function CustomersPage({searchParams}:{searchParams:Promise<{q?:string;status?:string;page?:string;message?:string;error?:string}>}){const [p,{activeMembership},supabase]=await Promise.all([searchParams,getDashboardContext(),createClient()]);const page=Math.max(1,Number(p.page)||1),archived=p.status==="archived",q=p.q?.trim().replace(/[,%()]/g," ").slice(0,100);let query=supabase.from("customers").select("id,full_name,phone,email,is_archived,updated_at,vehicles(count)",{count:"exact"}).eq("organization_id",activeMembership.organizationId).eq("is_archived",archived).order("updated_at",{ascending:false}).range((page-1)*PAGE_SIZE,page*PAGE_SIZE-1);if(q)query=query.or(`full_name.ilike.%${q}%,phone_normalized.ilike.%${q.replace(/\D/g,"")}%,email.ilike.%${q}%`);const{data,count,error}=await query,pages=Math.max(1,Math.ceil((count??0)/PAGE_SIZE)),canWrite=["owner","manager","advisor"].includes(activeMembership.role);return <div className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold text-amber-700">Customer CRM</p><h1 className="mt-1 text-3xl font-black">Customers</h1><p className="mt-2 text-zinc-600">Search contacts and open their vehicles in a few taps.</p></div>{canWrite&&<Button asChild><Link href="/dashboard/customers/new"><Plus size={18}/>Add customer</Link></Button>}</div><FormMessage message={p.message} error={p.error??(error?"Unable to load customers.":undefined)}/><form className="mt-6 grid gap-3 sm:grid-cols-[1fr_auto_auto]"><label className="relative"><span className="sr-only">Search customers</span><Search className="absolute left-3 top-3.5 text-zinc-400" size={18}/><input name="q" defaultValue={p.q} placeholder="Name, mobile, or email" className="min-h-11 w-full rounded-xl border border-zinc-200 bg-white pl-10 pr-3 text-zinc-950"/></label><select name="status" defaultValue={p.status??"active"} className="min-h-11 rounded-xl border border-zinc-200 bg-white px-3 text-zinc-950"><option value="active">Active</option><option value="archived">Archived</option></select><Button type="submit" variant="secondary">Search</Button></form><div className="mt-5 grid gap-3">{data?.map(c=><Link key={c.id} href={`/dashboard/customers/${c.id}`} className="rounded-2xl border border-zinc-200 bg-white p-5 text-zinc-950 shadow-sm hover:border-amber-300"><div className="flex items-start justify-between gap-4"><div><h2 className="font-black">{c.full_name}</h2><p className="mt-1 text-sm text-zinc-600">{displayPhone(c.phone)}{c.email?` · ${c.email}`:""}</p></div><span className="whitespace-nowrap text-sm font-bold text-zinc-600">{c.vehicles?.[0]?.count??0} vehicles</span></div><p className="mt-3 text-xs text-zinc-500">Updated {new Intl.DateTimeFormat("en-PH",{dateStyle:"medium"}).format(new Date(c.updated_at))}</p></Link>)}{!error&&data?.length===0&&<Card className="p-10 text-center"><h2 className="text-lg font-black">No {archived?"archived ":""}customers</h2><p className="mt-2 text-sm text-zinc-600">{q?"Try another search.":"Add your first customer to start building customer and vehicle records."}</p></Card>}</div>{pages>1&&<nav aria-label="Customer pages" className="mt-6 flex items-center justify-between"><Button asChild variant="secondary"><Link href={`?q=${encodeURIComponent(p.q??"")}&status=${p.status??"active"}&page=${Math.max(1,page-1)}`}>Previous</Link></Button><span className="text-sm text-zinc-600">Page {page} of {pages}</span><Button asChild variant="secondary"><Link href={`?q=${encodeURIComponent(p.q??"")}&status=${p.status??"active"}&page=${Math.min(pages,page+1)}`}>Next</Link></Button></nav>}</div>}
