import{notFound}from"next/navigation";import{saveCommunicationPreferences}from"@/app/dashboard/reminders/actions";import{FormMessage}from"@/components/form-message";import{SubmitButton}from"@/components/submit-button";import{Card}from"@/components/ui/card";import{getDashboardContext}from"@/lib/auth/context";import{createClient}from"@/lib/supabase/server";
export default async function Page({params,searchParams}:{params:Promise<{customerId:string}>;searchParams:Promise<{message?:string;error?:string}>}){const[{customerId},p,{activeMembership},supabase]=await Promise.all([params,searchParams,getDashboardContext(),createClient()]),[{data:customer},{data:preference}]=await Promise.all([supabase.from("customers").select("id,full_name,email,phone").eq("id",customerId).eq("organization_id",activeMembership.organizationId).maybeSingle(),supabase.from("customer_communication_preferences").select("*").eq("customer_id",customerId).maybeSingle()]);if(!customer)notFound();return <div className="mx-auto max-w-xl"><p className="text-sm font-bold text-amber-700">Customer consent</p><h1 className="text-3xl font-black">{customer.full_name}</h1><p className="mt-2 text-zinc-600">Record explicit consent before sending retention messages.</p><FormMessage {...p}/><Card className="mt-6 p-5"><form action={saveCommunicationPreferences} className="space-y-4"><input type="hidden" name="customerId" value={customer.id}/><label className="flex gap-3 rounded-xl border p-4"><input type="checkbox" name="emailOptIn" defaultChecked={preference?.email_opt_in}/><span><strong>Email reminders</strong><small className="block text-zinc-500">{customer.email||"No email address recorded"}</small></span></label><label className="flex gap-3 rounded-xl border p-4"><input type="checkbox" name="smsOptIn" defaultChecked={preference?.sms_opt_in}/><span><strong>SMS reminders</strong><small className="block text-zinc-500">{customer.phone||"No phone number recorded"}</small></span></label><p className="text-xs text-zinc-500">Clearing both options records an opt-out. No external provider sends messages unless it is separately configured.</p><SubmitButton pendingText="Saving…">Save preferences</SubmitButton></form></Card></div>}
