import { BranchForm } from "@/components/crm-forms"; export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <div className="mx-auto max-w-3xl"><p className="text-sm font-bold text-brand-primary">Branches</p><h1 className="mt-1 text-3xl font-black">Add branch</h1><div className="mt-6"><BranchForm error={p.error}/></div></div>}
