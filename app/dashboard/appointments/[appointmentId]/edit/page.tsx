import{notFound}from"next/navigation";import{VisitForm}from"@/components/operations-forms";import{getDashboardContext}from"@/lib/auth/context";import{getVisitChoices}from"@/lib/operations-data";import{inputDateTimeInZone}from"@/lib/operations";import{createClient}from"@/lib/supabase/server";
export default async function Page({params,searchParams}:{params:Promise<{appointmentId:string}>;searchParams:Promise<{customerQ?:string;error?:string}>}){const[{appointmentId},p,choices,{activeMembership},supabase]=await Promise.all([params,searchParams,getVisitChoices((await searchParams).customerQ),getDashboardContext(),createClient()]);const{data:a}=await supabase.from("appointments").select("id,branch_id,customer_id,vehicle_id,starts_at,customer_note,internal_note,status,appointment_services(service_id)").eq("id",appointmentId).eq("organization_id",activeMembership.organizationId).maybeSingle();if(!a||!["requested","confirmed"].includes(a.status))notFound();const branch=await supabase.from("branches").select("timezone").eq("id",a.branch_id).single();return <div className="mx-auto max-w-3xl"><p className="text-sm font-bold text-amber-700">Appointments</p><h1 className="mt-1 text-3xl font-black">Edit appointment</h1><div className="mt-6"><VisitForm mode="appointment" appointment={{...a,starts_at:inputDateTimeInZone(a.starts_at!,branch.data?.timezone??activeMembership.timezone),serviceIds:a.appointment_services.map(i=>i.service_id)}} branches={activeMembership.branches.map(b=>({id:b.id,name:b.name}))} customers={choices.customers} vehicles={choices.vehicles} services={choices.services} customerQ={p.customerQ} error={p.error}/></div></div>}
