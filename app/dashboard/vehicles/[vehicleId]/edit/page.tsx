import { notFound } from "next/navigation"; import { VehicleForm, type VehicleRecord } from "@/components/crm-forms"; import { getDashboardContext } from "@/lib/auth/context"; import { createClient } from "@/lib/supabase/server";
export default async function Page({params,searchParams}:{params:Promise<{vehicleId:string}>;searchParams:Promise<{error?:string;warning?:string;duplicateId?:string}>}){const[{vehicleId},p,{activeMembership},supabase]=await Promise.all([params,searchParams,getDashboardContext(),createClient()]);const[{data:v},{data:customers}]=await Promise.all([supabase.from("vehicles").select("id,customer_id,make,model,plate_number,model_year,variant,color,vehicle_type,fuel_type,transmission,odometer_km,vin,engine_number,notes").eq("id",vehicleId).eq("organization_id",activeMembership.organizationId).maybeSingle(),supabase.from("customers").select("id,full_name").eq("organization_id",activeMembership.organizationId).eq("is_archived",false).order("full_name").limit(500)]);if(!v)notFound();return <div className="mx-auto max-w-3xl"><p className="text-sm font-bold text-amber-700">Vehicles</p><h1 className="mt-1 text-3xl font-black">Edit vehicle</h1><div className="mt-6"><VehicleForm vehicle={v as VehicleRecord} customers={customers??[]} {...p}/></div></div>}
