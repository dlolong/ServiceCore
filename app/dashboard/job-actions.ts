"use server";
import { revalidatePath } from "next/cache";import{redirect}from"next/navigation";import{getDashboardContext}from"@/lib/auth/context";import{firstError,formValue}from"@/lib/crm";import{additionalWorkSchema,assignmentSchema,estimateSchema,inspectionSchema,paymentSchema,reversalSchema}from"@/lib/jobs";import{parseMoneyToCentavos,zonedDateTimeToUtc}from"@/lib/operations";import{createClient}from"@/lib/supabase/server";
function go(path:string,kind:"error"|"message",text:string):never{redirect(`${path}?${kind}=${encodeURIComponent(text)}`)}
export async function startQueueJob(data:FormData){const{supabase}=await getDashboardContext().then(async()=>({supabase:await createClient()}));const{data:id,error}=await supabase.rpc("convert_queue_to_job",{p_queue_id:formValue(data,"queueId")});if(error||!id)go("/dashboard/queue","error",error?.message??"Unable to start job.");revalidatePath("/dashboard");redirect(`/dashboard/jobs/${id}?message=Job+created.`)}
export async function transitionJob(data:FormData){const id=formValue(data,"jobId"),supabase=await createClient(),{error}=await supabase.rpc("transition_job",{p_job_id:id,p_action:formValue(data,"action")});if(error)go(`/dashboard/jobs/${id}`,"error",error.message);revalidatePath("/dashboard/jobs");go(`/dashboard/jobs/${id}`,"message","Job status updated.")}
export async function assignJob(data:FormData){const parsed=assignmentSchema.safeParse({jobId:formValue(data,"jobId"),technicianId:formValue(data,"technicianId"),promisedAt:formValue(data,"promisedAt")});if(!parsed.success)go(`/dashboard/jobs/${formValue(data,"jobId")}`,"error",firstError(parsed.error));const{activeMembership}=await getDashboardContext();if(!["owner","manager","advisor"].includes(activeMembership.role))go("/dashboard/jobs","error","Assignment access required.");const supabase=await createClient();let promised:string|null=null;if(parsed.data.promisedAt){const value=zonedDateTimeToUtc(parsed.data.promisedAt,activeMembership.timezone);if(!value)go(`/dashboard/jobs/${parsed.data.jobId}`,"error","Invalid promised time.");promised=value.toISOString();}const{error}=await supabase.rpc("assign_job",{p_job_id:parsed.data.jobId,p_technician_id:parsed.data.technicianId||null,p_promised_at:promised});if(error)go(`/dashboard/jobs/${parsed.data.jobId}`,"error","Unable to assign technician.");go(`/dashboard/jobs/${parsed.data.jobId}`,"message","Assignment updated.")}
export async function saveInspection(data:FormData){const parsed=inspectionSchema.safeParse({jobId:formValue(data,"jobId"),odometerIn:formValue(data,"odometerIn"),fuelLevel:formValue(data,"fuelLevel"),exteriorNotes:formValue(data,"exteriorNotes"),interiorNotes:formValue(data,"interiorNotes"),tireNotes:formValue(data,"tireNotes"),lightNotes:formValue(data,"lightNotes"),windshieldNotes:formValue(data,"windshieldNotes"),damageNotes:formValue(data,"damageNotes"),warningNotes:formValue(data,"warningNotes"),belongingsNote:formValue(data,"belongingsNote")});const back=`/dashboard/jobs/${formValue(data,"jobId")}`;if(!parsed.success)go(back,"error",firstError(parsed.error));const{activeMembership,user}=await getDashboardContext(),supabase=await createClient();const{error:jobError}=await supabase.rpc("update_job_details",{p_job_id:parsed.data.jobId,p_technician_id:null,p_promised_at:null,p_odometer_in:parsed.data.odometerIn||null,p_fuel_level:parsed.data.fuelLevel||null});const payload={organization_id:activeMembership.organizationId,job_order_id:parsed.data.jobId,exterior_notes:parsed.data.exteriorNotes||null,interior_notes:parsed.data.interiorNotes||null,tire_notes:parsed.data.tireNotes||null,light_notes:parsed.data.lightNotes||null,windshield_notes:parsed.data.windshieldNotes||null,damage_notes:parsed.data.damageNotes||null,warning_indicator_notes:parsed.data.warningNotes||null,belongings_note:parsed.data.belongingsNote||null,inspected_by:user.id};const{error}=await supabase.from("job_inspections").upsert(payload,{onConflict:"job_order_id"});if(jobError||error)go(back,"error","Unable to save inspection.");revalidatePath(back);go(back,"message","Inspection saved.")}
export async function uploadJobPhoto(data:FormData){const jobId=formValue(data,"jobId"),category=formValue(data,"category"),file=data.get("photo");const{activeMembership,user}=await getDashboardContext();if(!(file instanceof File)||!file.size||file.size>10*1024*1024||!["image/jpeg","image/png","image/webp"].includes(file.type))go(`/dashboard/jobs/${jobId}`,"error","Choose a JPG, PNG, or WebP image up to 10 MB.");const supabase=await createClient(),extension=file.name.split(".").pop()?.replace(/[^a-z0-9]/gi,"").toLowerCase()||"jpg",path=`${activeMembership.organizationId}/${jobId}/${crypto.randomUUID()}.${extension}`,{error:uploadError}=await supabase.storage.from("job-photos").upload(path,file,{contentType:file.type,upsert:false});if(uploadError)go(`/dashboard/jobs/${jobId}`,"error","Photo upload failed.");const{error}=await supabase.from("job_photos").insert({organization_id:activeMembership.organizationId,job_order_id:jobId,category,storage_path:path,uploaded_by:user.id});if(error){await supabase.storage.from("job-photos").remove([path]);go(`/dashboard/jobs/${jobId}`,"error","Unable to attach photo.");}revalidatePath(`/dashboard/jobs/${jobId}`);go(`/dashboard/jobs/${jobId}`,"message","Photo uploaded.")}
export async function createEstimate(data:FormData){const parsed=estimateSchema.safeParse({jobId:formValue(data,"jobId"),discount:formValue(data,"discount"),tax:formValue(data,"tax"),notes:formValue(data,"notes")}),back=`/dashboard/jobs/${formValue(data,"jobId")}`;if(!parsed.success)go(back,"error",firstError(parsed.error));const discount=parseMoneyToCentavos(parsed.data.discount||"0"),tax=parseMoneyToCentavos(parsed.data.tax||"0");if(discount===null||tax===null)go(back,"error","Enter valid discount and tax amounts.");const supabase=await createClient(),{data:id,error}=await supabase.rpc("create_estimate",{p_job_id:parsed.data.jobId,p_discount_centavos:Number(discount),p_tax_centavos:Number(tax),p_notes:parsed.data.notes||null});if(error||!id)go(back,"error",error?.message??"Unable to create estimate.");redirect(`/dashboard/estimates/${id}?message=Estimate+created.`)}
export async function transitionEstimate(data:FormData){const id=formValue(data,"estimateId"),supabase=await createClient(),{error}=await supabase.rpc("transition_estimate",{p_estimate_id:id,p_action:formValue(data,"action"),p_note:formValue(data,"note")||null});if(error)go(`/dashboard/estimates/${id}`,"error",error.message);go(`/dashboard/estimates/${id}`,"message","Estimate updated.")}
export async function issueInvoice(data:FormData){const jobId=formValue(data,"jobId"),estimateId=formValue(data,"estimateId")||null,supabase=await createClient(),{data:id,error}=await supabase.rpc("issue_invoice",{p_job_id:jobId,p_estimate_id:estimateId,p_notes:null});if(error||!id)go(`/dashboard/jobs/${jobId}`,"error",error?.message??"Unable to issue invoice.");redirect(`/dashboard/invoices/${id}?message=Invoice+issued.`)}
export async function recordPayment(data:FormData){const parsed=paymentSchema.safeParse({invoiceId:formValue(data,"invoiceId"),amount:formValue(data,"amount"),method:formValue(data,"method"),reference:formValue(data,"reference"),notes:formValue(data,"notes")}),back=`/dashboard/invoices/${formValue(data,"invoiceId")}`;if(!parsed.success)go(back,"error",firstError(parsed.error));const amount=parseMoneyToCentavos(parsed.data.amount);if(amount===null)go(back,"error","Enter a valid payment amount.");const supabase=await createClient(),{data:id,error}=await supabase.rpc("record_invoice_payment",{p_invoice_id:parsed.data.invoiceId,p_amount_centavos:Number(amount),p_method:parsed.data.method,p_reference:parsed.data.reference||null,p_notes:parsed.data.notes||null});if(error||!id)go(back,"error",error?.message??"Unable to record payment.");revalidatePath(back);go(back,"message","Payment recorded and receipt created.")}
export async function addJobService(data:FormData){const parsed=additionalWorkSchema.safeParse({jobId:formValue(data,"jobId"),serviceId:formValue(data,"serviceId"),quantity:formValue(data,"quantity"),requiresApproval:data.get("requiresApproval")==="on",notes:formValue(data,"notes")}),back=`/dashboard/jobs/${formValue(data,"jobId")}`;if(!parsed.success)go(back,"error",firstError(parsed.error));const supabase=await createClient(),{error}=await supabase.rpc("add_job_service",{p_job_id:parsed.data.jobId,p_service_id:parsed.data.serviceId,p_quantity:parsed.data.quantity,p_requires_approval:parsed.data.requiresApproval,p_notes:parsed.data.notes||null});if(error)go(back,"error",error.message);revalidatePath(back);go(back,"message","Additional work added.")}
export async function transitionJobService(data:FormData){const jobId=formValue(data,"jobId"),supabase=await createClient(),{error}=await supabase.rpc("transition_job_service",{p_item_id:formValue(data,"itemId"),p_action:formValue(data,"action")});if(error)go(`/dashboard/jobs/${jobId}`,"error",error.message);revalidatePath(`/dashboard/jobs/${jobId}`);go(`/dashboard/jobs/${jobId}`,"message","Proposed work updated.")}
export async function reversePayment(data:FormData){const parsed=reversalSchema.safeParse({paymentId:formValue(data,"paymentId"),action:formValue(data,"action"),note:formValue(data,"note")}),invoiceId=formValue(data,"invoiceId"),back=`/dashboard/invoices/${invoiceId}`;if(!parsed.success)go(back,"error",firstError(parsed.error));const supabase=await createClient(),{error}=await supabase.rpc("reverse_payment",{p_payment_id:parsed.data.paymentId,p_action:parsed.data.action,p_note:parsed.data.note});if(error)go(back,"error",error.message);revalidatePath(back);go(back,"message",`Payment ${parsed.data.action} recorded.`)}
export async function voidInvoice(data:FormData){const invoiceId=formValue(data,"invoiceId"),supabase=await createClient(),{error}=await supabase.rpc("void_invoice",{p_invoice_id:invoiceId,p_reason:formValue(data,"reason")});if(error)go(`/dashboard/invoices/${invoiceId}`,"error",error.message);revalidatePath(`/dashboard/invoices/${invoiceId}`);go(`/dashboard/invoices/${invoiceId}`,"message","Invoice voided.")}
export async function assignJobItem(data:FormData){const jobId=formValue(data,"jobId"),supabase=await createClient(),{error}=await supabase.rpc("assign_job_item",{p_item_id:formValue(data,"itemId"),p_technician_id:formValue(data,"technicianId")||null});if(error)go(`/dashboard/jobs/${jobId}`,"error","Unable to assign service technician.");revalidatePath(`/dashboard/jobs/${jobId}`);go(`/dashboard/jobs/${jobId}`,"message","Service assignment updated.")}
