import{acceptInvitation}from"@/app/accept-invite/actions";import{FormMessage}from"@/components/form-message";import{SubmitButton}from"@/components/submit-button";import{Card}from"@/components/ui/card";import{requireAuthenticatedUser}from"@/lib/auth/context";
export default async function Page({searchParams}:{searchParams:Promise<{token?:string;error?:string}>}){const p=await searchParams,token=p.token??"";await requireAuthenticatedUser(`/accept-invite?token=${encodeURIComponent(token)}`);return <main className="grid min-h-screen place-items-center bg-zinc-100 p-5"><Card className="w-full max-w-md p-6"><p className="text-sm font-bold text-amber-700">Staff invitation</p><h1 className="mt-2 text-3xl font-black">Join this KarKR shop</h1><p className="mt-2 text-sm text-zinc-600">Accepting adds your signed-in account with the role and branch access chosen by the shop owner.</p><FormMessage error={p.error}/><form action={acceptInvitation} className="mt-6"><input type="hidden" name="token" value={token}/><SubmitButton pendingText="Accepting…">Accept invitation</SubmitButton></form></Card></main>}
